#include <stdio.h>
int main(void) 
{
	int n;
	scanf("%d",&n);
	if(n<0)
	{
		printf("if the number is positive");
	}
	else if(n==0)
	{
		printf("if the number is zero");
	}
	else
	{
		printf("if the number is negatie");
	}
}
